<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>My Portfolio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #0f5132, #198754);
            color: white;
            overflow-x: hidden;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 25px 8%;
        }

        header h2 {
            font-weight: 700;
            letter-spacing: 1px;
        }

        nav a {
            color: white;
            margin-left: 25px;
            text-decoration: none;
            font-weight: 400;
            opacity: 0.8;
        }

        nav a:hover {
            opacity: 1;
        }

        .hero {
            min-height: calc(100vh - 100px);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 8%;
        }

        .hero-text {
            max-width: 520px;
        }

        .hero-text h1 {
            font-size: 48px;
            font-weight: 700;
        }

        .hero-text h1 span {
            color: #20c997;
        }

        .hero-text h3 {
            font-weight: 400;
            margin: 15px 0;
            opacity: 0.9;
        }

        .hero-text p {
            font-size: 15px;
            line-height: 1.7;
            opacity: 0.8;
            margin-bottom: 25px;
        }

        .buttons button {
            padding: 12px 28px;
            border-radius: 30px;
            border: none;
            cursor: pointer;
            font-size: 15px;
            margin-right: 15px;
        }

        .btn-main {
            background: #20c997;
            color: #003d2b;
            font-weight: 600;
        }

        .btn-outline {
            background: transparent;
            border: 1px solid #20c997;
            color: #20c997;
        }

        .socials i {
            margin-right: 15px;
            font-size: 18px;
            opacity: 0.8;
            cursor: pointer;
        }

        .socials i:hover {
            opacity: 1;
        }

        .hero-img {
            position: relative;
        }

        .hero-img img {
            width: 320px;
            border-radius: 30px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.4);
        }

        .blur {
            position: absolute;
            width: 260px;
            height: 260px;
            background: #20c997;
            filter: blur(120px);
            z-index: -1;
            top: 50px;
            left: 50px;
        }

        @media(max-width: 900px) {
            .hero {
                flex-direction: column;
                text-align: center;
            }

            .hero-img {
                margin-top: 40px;
            }
        }
    </style>
</head>
<body>

<header>
    <h2>⭐ MyBrand</h2>
    <nav>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Projects</a>
        <a href="#">Contact</a>
    </nav>
</header>

<section class="hero">
    <div class="hero-text">
        <h1>Mình là <span>Quyên</span></h1>
        <h3>I am a CREATOR</h3>
        <p>
            "Trong cuộc sống, ta không thật sự cần lời khuyên từ người khác.
            Họ chỉ hiểu khi chính mình trải qua."
        </p>

        <div class="buttons">
            <button class="btn-main">Review</button>
            <button class="btn-outline">Donate</button>
        </div>

        <div class="socials" style="margin-top:25px;">
            <i class="fab fa-facebook"></i>
            <i class="fab fa-instagram"></i>
            <i class="fab fa-tiktok"></i>
            <i class="fab fa-youtube"></i>
        </div>
    </div>

    <div class="hero-img">
        <div class="blur"></div>
        <img src="https://images.unsplash.com/photo-1527980965255-d3b416303d12">
    </div>
</section>

</body>
</html>
